{
  "branches": ["main"],
  "tagFormat": "v${version}",
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "mkdir -p release-assets && cp dist/Cadence-unsigned.ipa release-assets/Cadence-${nextRelease.version}-unsigned.ipa && cp dist/Cadence-simulator.app.zip release-assets/Cadence-${nextRelease.version}-simulator.app.zip"
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": [
          { "path": "release-assets/*-unsigned.ipa", "label": "Unsigned device build (re-sign to sideload)" },
          { "path": "release-assets/*-simulator.app.zip", "label": "iOS Simulator app" }
        ],
        "successComment": false,
        "failComment": false
      }
    ]
  ]
}
